---
import Layout from '../layouts/Layout.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import JobCard from '../components/JobCard.astro';
import Newsletter from '../components/Newsletter.astro';
import jobs from '../data/jobs.json';

const featuredJobs = jobs.slice(0, 10);
---

<Layout title="designwith.care — Healthcare UX Jobs">
  <Header />

  <!-- Hero -->
  <section class="max-w-6xl mx-auto px-6 pt-20 pb-16 text-center">
    <div class="inline-flex items-center gap-2 bg-sapphire-50 text-sapphire-500 text-sm font-medium px-4 py-1.5 rounded-full mb-6">
      <span>♥</span> Open source &amp; community-driven
    </div>
    <h1 class="text-4xl md:text-5xl lg:text-6xl font-bold text-gray-900 leading-tight max-w-3xl mx-auto">
      Healthcare UX jobs,<br />curated with
      <span class="text-sapphire-500">care</span>
    </h1>
    <p class="text-lg text-gray-500 mt-6 max-w-xl mx-auto leading-relaxed">
      The best design roles in health tech — sourced by the community, open to everyone.
    </p>
    <div class="flex justify-center gap-4 mt-8">
      <a
        href="/jobs"
        class="px-6 py-3 bg-sapphire-500 text-white font-semibold rounded-full hover:bg-sapphire-600 transition-colors text-sm"
      >
        Browse all jobs
      </a>
      <a
        href="https://github.com/designwithcare/jobs"
        target="_blank"
        class="px-6 py-3 border border-gray-200 text-gray-700 font-semibold rounded-full hover:border-sapphire-200 hover:text-sapphire-500 transition-all text-sm"
      >
        Submit a listing
      </a>
    </div>
  </section>

  <!-- Value Props -->
  <section class="max-w-6xl mx-auto px-6 pb-20">
    <div class="grid md:grid-cols-3 gap-6">
      <div class="bg-white rounded-2xl p-8 border border-creme-300/60">
        <div class="text-3xl mb-4">🎯</div>
        <h3 class="text-lg font-semibold text-gray-900 mb-2">Curated, not scraped</h3>
        <p class="text-sm text-gray-500 leading-relaxed">
          Every listing is reviewed by the community. No spam, no ghost jobs — only real roles at companies doing meaningful work in healthcare.
        </p>
      </div>
      <div class="bg-white rounded-2xl p-8 border border-creme-300/60">
        <div class="text-3xl mb-4">🌱</div>
        <h3 class="text-lg font-semibold text-gray-900 mb-2">Open source</h3>
        <p class="text-sm text-gray-500 leading-relaxed">
          The entire job board runs on GitHub. Anyone can submit a listing via pull request. Transparent, free, and community-owned.
        </p>
      </div>
      <div class="bg-white rounded-2xl p-8 border border-creme-300/60">
        <div class="text-3xl mb-4">💙</div>
        <h3 class="text-lg font-semibold text-gray-900 mb-2">Design for impact</h3>
        <p class="text-sm text-gray-500 leading-relaxed">
          Healthcare UX is one of the most impactful design domains. Find roles where your work genuinely improves people's lives.
        </p>
      </div>
    </div>
  </section>

  <!-- Featured Jobs -->
  <section class="max-w-6xl mx-auto px-6 pb-20">
    <div class="flex items-center justify-between mb-8">
      <h2 class="text-2xl font-bold text-gray-900">Latest jobs</h2>
      <a href="/jobs" class="text-sm font-medium text-sapphire-500 hover:text-sapphire-600 transition-colors">
        View all →
      </a>
    </div>
    <div class="grid gap-3">
      {featuredJobs.map(job => (
        <JobCard {...job} />
      ))}
    </div>
    <div class="text-center mt-8">
      <a
        href="/jobs"
        class="inline-flex px-6 py-3 bg-sapphire-500 text-white font-semibold rounded-full hover:bg-sapphire-600 transition-colors text-sm"
      >
        View all {jobs.length} jobs →
      </a>
    </div>
  </section>

  <!-- Newsletter -->
  <section class="max-w-3xl mx-auto px-6 pb-20">
    <Newsletter />
  </section>

  <Footer />
</Layout>
